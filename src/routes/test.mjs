import {Simulator} from './sim.mjs'

const sim = new Simulator();
sim.createNewDefaultAddressSpace();

// sim.editSegment(sim.pas.segmentList[0], 2048, 256);
// sim.editSegment(sim.pas.segmentList[0], 0, 256);